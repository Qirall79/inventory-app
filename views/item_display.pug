extends layout 

block content
    h1= title 

    ul 
        li #[strong Description: ] #{item.description}
        li #[strong Category: ]
            a(href=item.category.url) #{item.category.name}
        li #[strong Price: ] #{"$" + item.price}
        li #[strong Number in Stock: ] #{item.number_in_stock}
        li 
            if (item.img==undefined)
                p #[strong Image not found]
            else 
                img(src=`data:image/${item.img.contentType};base64, ${(item.img.data ? item.img.data : "").toString("base64")}`, alt="image")
    hr
    p 
        a(href=`${item.url}/update`) Update
    p 
        a(href=`${item.url}/delete`) Delete