extends layout 

block content 

    h1= title 
    p #[strong Are you sure you want to delete this item ?]
    form(action="", method="post")
        input(type="hidden", name="id", value=item_id)
        input(type="submit", value="Delete") 